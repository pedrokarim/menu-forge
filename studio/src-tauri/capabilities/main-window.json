{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Fenêtre principale : sélecteur de dossier natif et « montrer dans l’explorateur », rien de plus. L’interface est servie par le backend local (http://127.0.0.1:<port>), d’où l’autorisation de cette origine distante.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "permissions": [
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir"
  ]
}
