{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Fenêtre principale : sélecteur de dossier natif et « montrer dans l’explorateur », rien de plus. L’interface est servie par le backend local (http://127.0.0.1:<port>) en production, et par Vite (http://localhost:5173) sous « tauri dev », d’où l’autorisation de ces origines distantes.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*/*", "http://localhost:5173/*"]
  },
  "permissions": [
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-is-maximized",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-title",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
